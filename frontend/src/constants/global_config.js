const GlobalSettings = {
  transactions: {
    source: "mock",
    enabled: true,
  },
  signers: {
    source: "mock",
    enabled: true,
  },
  vault: {
    source: "mock",
    enabled: true,
  },
  header: {
    enabled: true,
  },
  dev_mode: {
    enabled: true,
  },
};

export { GlobalSettings };
